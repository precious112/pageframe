#include <assert.h>
#include <stdio.h>

int main(void) {
    /* Placeholder test — replace with real tests. */
    assert(1 == 1);

    printf("test_example: all assertions passed\n");
    return 0;
}
